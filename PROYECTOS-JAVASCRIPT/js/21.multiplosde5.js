
const limit = 100; // Límite superior del rango
const divisor = 5; // Número del cual contar los múltiplos

const quantity = Math.floor(limit / divisor);

console.log("Número de múltiplos de 5 entre 1 y 100:", quantity);

// Alternativa para imprimir todos los múlt
